# ============================================
# agent/ package
# ============================================
# This file makes the agent/ folder a Python package.
# A Python package is a folder that Python treats as a
# collection of related modules (files) that can import
# each other.
#
# Without this file, Python would not recognize agent/
# as a package, and "from agent import config" would fail.
# ============================================
